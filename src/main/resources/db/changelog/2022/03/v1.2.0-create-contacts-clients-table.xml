<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.5.xsd">

    <changeSet id="1.2.0" author="miguelromero717">
        <sql>
            create table if not exists contacts_x_clients
            (
                contact_id bigserial
                constraint contacts_x_clients_contacts_id_fk
                references contacts
                constraint contacts_x_clients_fk
                references clients,
                client_id  bigserial,
                constraint contacts_x_clients_pk
                primary key (client_id, contact_id)
                );

            alter table contacts_x_clients
                owner to superchat;
        </sql>
    </changeSet>

</databaseChangeLog>